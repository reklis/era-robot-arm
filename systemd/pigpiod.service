[Unit]
Description=Daemon for the pigpio GPIO library
After=network.target syslog.target
Wants=network.target

[Service]
ExecStart=/usr/local/bin/pigpiod -x /dev/null
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
Type=simple

[Install]
WantedBy=multi-user.target
